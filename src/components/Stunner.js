import React from 'react'

//import navigation data
import {stunnerData} from '../data';

const Stunner = () => {
    return (
        <section id='stunner'>
            STUNner
        </section>
    );
};

export default Stunner;