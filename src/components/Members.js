import React from 'react'

//import navigation data
import {membersData} from '../data';

const Members = () => {
    return (
        <section id='members'>
            Members
        </section>
    );
};

export default Members;