import React from 'react'

//import about data
import {aboutData} from '../data';

const About = () => {
    return (
        <section>
            About
        </section>
    );
};

export default About;